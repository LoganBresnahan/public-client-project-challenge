<%= form_for :thisdoesntmatter, url: user_stashes_path(current_user), id: game.id do |f|%>
  <input type="hidden" name="id" value="<%= game.id %>">
  <%= f.button "Add", class: "btn btn-primary" %>
<% end %>
