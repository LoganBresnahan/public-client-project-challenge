<%= form_for :stash, method: :delete, url: user_stash_path(current_user, user_stash(game)) do |f|%>
  <input type="hidden" name="game_id" value="<%= game.id %>">
  <%= f.button "Remove", class: "btn btn-danger" %>
<% end %>
