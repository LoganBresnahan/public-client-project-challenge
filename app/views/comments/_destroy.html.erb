<%= form_for :comment, method: :delete, url: game_comment_path(@game, comment.id), confirm: "Are you sure?" do |f| %>
  <input type='hidden' name='game_id' value="<%= @game.id %>">

  <%= f.button "Delete", class: "btn btn-primary" %>
<% end %>